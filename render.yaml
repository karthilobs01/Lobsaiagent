services:
  - type: web
    name: your-voice-agent
    env: python
    buildCommand: |
      apt-get update && apt-get install -y portaudio19-dev
      pip install -r requirements.txt
    startCommand: python agent.py console # replace with your actual script name
